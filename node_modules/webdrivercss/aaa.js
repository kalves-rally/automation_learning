// init WebdriverJS
var client = require('webdriverio').remote({
    // port: 4723,
    // host: 'ondemand.saucelabs.com',
    // port: 80,
    // user: process.env.SAUCE_USERNAME,
    // key: process.env.SAUCE_ACCESS_KEY,
    logLevel:'command',
    desiredCapabilities: {
        'browserName': 'chrome',
        chromeOptions: { mobileEmulation: { deviceName: 'Apple iPad 3 / 4' } }
    }
});

// init WebdriverCSS
var plugin = require('./index').init(client);

client
    .init()
    .url('http://github.com')
    .webdrivercss('startpage', [{
        name: 'header',
        elem: '.header-logged-out'
    }, {
        name: 'form',
        elem: '.js-form-signup-home'
    }], function(err, res) {

        if(err) {
            return console.log('Ups', err);
        }
        
        console.log(res);
    })
    .end();
